<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.faisal.catatankeuangan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"/>
        <activity android:name=".pemasukan.PemasukanUpdateDeleteActivity" />
        <activity android:name=".pemasukan.PemasukanListActivty" />
        <activity android:name=".pemasukan.PemasukanInputActivity" />
        <activity android:name=".pengeluaran.PengeluaranUpdateDeleteActivity"/>
        <activity android:name=".pengeluaran.PengeluaranListActivty"/>
        <activity android:name=".pengeluaran.PengeluaranInputActivity"/>
        <activity android:name=".GoogleSignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>