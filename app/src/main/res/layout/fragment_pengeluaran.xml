<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".pemasukan.PemasukanFragment">

    <Button
        style="@style/button_view"
        android:drawableTint="@color/white"
        android:drawableLeft="@drawable/ic_add"
        android:layout_marginTop="10dp"
        android:padding="25dp"
        android:id="@+id/btn_catatan_pemasukan"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/catatan_pengeluaran" />
    <Button
        style="@style/button_view"
        android:drawableTint="@color/white"
        android:drawableLeft="@drawable/ic_list"
        android:padding="25dp"
        android:layout_marginTop="20dp"
        android:id="@+id/btn_list_pemasukan"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/daftar_pengeluaran"
        />

</LinearLayout>